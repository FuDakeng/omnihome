# 万事屋 CHANGELOG

## 0.3.11 — 2026-09-18

- 优化：密码保险库排版对齐快捷导航，左侧分类、右侧凭据卡片
- 新增：分类多选、拖拽排序、拖到底部条快速删除
- 新增：凭据卡片可按加入时间 / 更新时间 / 名称排序，支持多选、拖拽排序、拖入分类、拖拽删除
- 优化：同一网站可保存多个账号，卡片上标明同站账号数

## 0.3.10 — 2026-09-18

- 新增：密码保险库可创建分类管理凭据（分类名称随主密码加密，不落明文）
- 优化：点击预览同时显示地址、账号、密码，并可分别复制

## 0.3.9 — 2026-09-18

- 修复：编辑布局时灵感速记、今日计划等组件的拖拽/收纳按钮被卡片裁掉，并盖住标题栏按钮

## 0.3.8 — 2026-09-17

- 修复：编辑布局时组件无法拖动换位（HTML5 拖放在源卡片移出网格后被浏览器取消）

## 0.3.7 — 2026-09-17

- 优化：仪表盘同一行组件可前后互换位置（不再只能往前插）
- 优化：拉宽/拉高后正文、日历、监控曲线等随卡片铺开
- 修复：纵向拉高某张卡片时，不再把同一行其它卡片一起撑高

## 0.3.6 — 2026-09-17
- 新增：翻译、强密码生成可作为仪表盘组件，编辑布局后从「添加组件」放入
- 优化：JSON 格式化入口改到工具箱页（原先只在仪表盘工具格）

## 0.3.5 — 2026-09-16

- 修复：设置 → 外观的「界面密度」「降低动效」只改控件、不改界面（缺 CSS，且要等保存接口才套到页面）
- 优化：外观页拆开主题 / 主色卡片，行距与分割线留白加大，PC 上不再贴在一起

## 0.3.4 — 2026-09-15

- 修复：知识库笔记、文件夹拖到回收站无法删除（ESM 拆分后拖拽状态读了未定义变量）
- 优化：设置 → 关于的更新日志改为固定高度，超出内容内部滚动，下方「开源与致谢」不再被顶出视口

## 0.3.3 — 2026-09-11

- 修复：手机端知识库编辑器底部面包屑与字数统计被遮挡，正文区可收缩，底栏留在可视范围
- 优化：手机去掉常驻底部导航，只保留顶栏汉堡打开的侧栏抽屉（与 PC 同一套导航）
- 工程：补充 PC / 手机 UI 双向适配约束，后续改界面需两侧一起验

## 0.3.2 — 2026-09-11

- 修复：仪表盘「灵感速记」正文过长时卡片无限拉高，现固定高度，超出部分滚动

## 0.3.1 — 2026-09-11

- 修复：工具箱翻译英译中常把英文原文当译文（看起来没有作用）
- 优化：翻译改为 Google / 有道 / MyMemory 并行竞速，连接超时缩短，不再先卡 Google 再串行回退

## 0.3.0 — 2026-09-10

- 新增：手机版 UI（≤768px Phone Shell）——隐藏常驻侧栏、抽屉菜单、底栏导航、紧凑顶栏与搜索展开
- 新增：知识库与分享页在手机上改为列表 / 编辑器两级，大纲改为底部面板，默认实时模式
- 优化：触控下笔记/书签操作钮常显；保险库解锁、设置中心、监控表格、仪表盘工具格适配窄屏
- 优化：安全区、100dvh、输入框 16px，避免 iOS 缩放与底栏遮挡

## 0.2.52 — 2026-09-10

- 工程化：按域拆分 FastAPI 路由与存储引擎内部实现，保持原 API 契约
- 工程化：前端改为原生 ESM，主视图 hash 路由，静态资源版本戳统一从 VERSION 注入
- 工程化：会话改存 SQLite（WAL，可多 worker 共享）；文件写入改为按路径锁
- 安全：默认不挂载 docker.sock；SPA 下发 CSP；扩展不再保存登录密码
- 测试：回归迁入 pytest，依赖锁定，CI 门禁

## 0.2.51 — 2026-09-08

- 新增：设置中心 → 关于可导出详细系统运行日志（诊断 zip，含 API 访问记录与同步日志，密钥已打码）
- 修复：Obsidian 插件「用当前仓库覆盖服务端」在创建人仓库报「请求失败 400」；创建人无需再走审批（插件 0.0.9）

## 0.2.50 — 2026-09-08

- 优化：实时 Markdown 编辑器复制/剪切写入源码（如 - 123），粘到其它编辑器不再变成渲染符号（如 · 123）

## 0.2.49 — 2026-09-07

- 修复：无序列表等带标记的行按 ⌘/Ctrl+A 会卡在当前行，无法扩大到整个列表再全文

## 0.2.48 — 2026-09-07

- 修复：选中文本后再点查找、查找/替换输入框或工具栏按钮，编辑器选区会被取消；现保住选区，查找条打开时用高亮标出选中范围

## 0.2.47 — 2026-09-07

- 优化：实时编辑时光标不在 ``` 围栏标记上时显示代码块复制/折叠按钮；预览与只读模式始终显示
- 优化：Ctrl/⌘+A 改为阶梯全选——第一次选中当前行，第二次选中当前段落或代码块，第三次选中全文
- 优化：已选中文本时，查找与替换只在选区内进行，查找条显示「选区内」

## 0.2.46 — 2026-09-07

- 修复：知识库点击文档大纲（含标题跳转）不再强制把编辑器切到分屏，实时模式下直接滚到对应标题
- 新增：笔记 ⋯ 菜单「版本管理」——历史版本列表（版本号 / 客户端或服务端 / 保存时间），默认差异对比当前内容（新增/修改/删除），可关差异查看全文，并支持恢复

## 0.2.45 — 2026-09-07

- 修复：知识库实时 Markdown 编辑器选中文本后粘贴，会插到选区前面而不是覆盖选中内容

## 0.2.44 — 2026-09-06

- 优化：Obsidian 插件同步日志改为与万事屋一致的笔记级记录（新增/修改/删除，含名称、方向、时间、摘要）；服务端关闭该仓同步时提示「万事屋服务端已关闭当前笔记仓库的同步功能」（插件 0.0.8）
- 优化：团队仓库成员管理的编辑权限改为开关，并增加「编辑权限」表头
- 变更：创建人开启仓库同步开关后，拥有编辑权限的成员可自行生成同步令牌，无需创建人先发令牌
- 优化：知识库仓库功能菜单（⋯ / 仓库切换 / 笔记与文件夹操作）再次点击同一按钮可关闭

## 0.2.43 — 2026-09-06

- 新增：同步日志改为笔记级变更记录（新增/修改/删除），含笔记名、同步方向、时间与内容摘要
- 变更：Obsidian 插件下载按钮移到同步弹层底部，并在「设置 → 功能设置」保留同一入口
- 新增：按笔记仓库独立开关同步；关闭后该仓同步不可用、令牌失效但不吊销，重开后可继续使用
- 优化：生成同步令牌改为醒目警告「请保管好同步令牌，向他人提供可能导致数据泄漏！」

## 0.2.42 — 2026-09-06

- 变更：Obsidian 插件同步从「设置 → 功能设置」迁到知识库仓库 ⋯ 菜单弹层，令牌改为只配置当前仓库
- 优化：系统内置仓库与只读团队仓打开同步页时，「启用 Obsidian 同步」无法打开，并给出原因提示

## 0.2.41 — 2026-09-05

- 修复：创建人团队仓同步令牌与默认仓令牌互相覆盖（重置一个两个一起变），现按仓库独立签发；吊销只影响自己的钥，不波及团队成员
- 修复：团队编辑成员同步文件夹及其中笔记时，若创建人默认仓已有同名目录，文件夹不会被认领、笔记在团队仓目录树消失
- 优化：设置页各仓库令牌显示前缀，复制的连接信息带 vault；Obsidian 插件 0.0.6 对 4xx 不再误报「无法连接服务端」

## 0.2.40 — 2026-09-05

- 修复：自建仓转为团队仓后误报「默认仓库与系统内置仓库不能转换」，弹层停在转换页
- 优化：未生成或已吊销同步令牌的仓库只显示「生成令牌」，不再露出复制/重置/吊销
- 优化：转为团队仓后展示邀请码与链接；知识库 ⋯ 菜单可输入邀请码加入团队仓库
- 优化：团队仓库使用多人图标，与普通仓库区分
- 优化：仓库切换菜单只保留切换与新建；重命名/管理团队/删除改到 ⋯ 菜单，并去掉多选模式入口

## 0.2.39 — 2026-09-05

- 新增：分享页按文件夹树展示子目录（含空文件夹），不再扁平丢失层级
- 优化：仓库切换菜单可重命名、删除当前仓库（删除仅自建仓/团队仓创建人可见）
- 新增：团队笔记仓库——自建仓可转换，邀请链接（/?join=）、成员只读/可编辑、创建人托管笔记
- 新增：顶栏铃铛通知；成员高危同步（批量删服务端 >10 篇、用本地覆盖服务端）需创建人审批
- Obsidian 插件 0.0.5：可编辑成员可发自己的同步钥；批量删除/覆盖服务端先提交审批，获票后再执行

## 0.2.38 — 2026-09-05

- 修复：分享页分屏与预览无区别（源码框带 hidden，分屏也看不到编辑区）
- 修复：密码错误被前端一律报成「未登录或会话已过期」
- 修复：Safari 打开需登录分享链接时，boot 把登录页又藏掉导致白屏
- 优化：已分享不再额外占位显示「享」，改为在笔记/文件夹图标上角标

## 0.2.37 — 2026-09-05

- 修复：登录会话仅存在内存，容器重启后立刻掉线；现已写入 data/sessions.json，访问时滑动续期 7 天
- 修复：分享页被 420px 模态宽度压成竖条；现铺满宽屏
- 修复：取消分享只删当前链接，同笔记/文件夹的旧分享仍显示「享」；现一并吊销
- 新增：分享页可下载当前笔记 Markdown，多篇时可打包 zip

## 0.2.36 — 2026-09-04

- 修复：已有分享改「允许编辑」不会写回链接权限；现在开关/有效期立即更新当前链接，无需重新生成
- 新增：分享页可开关文档大纲；可编辑时显示格式工具栏与实时 / 分屏 / 预览

## 0.2.35 — 2026-09-04

- 修复：无需登录的分享链接被登录页挡住（登录层 z-index 高于分享层，未登录 boot 一律 lock）
- 新增：再次打开已分享笔记/文件夹时回显上次生成的链接及权限、有效期

## 0.2.34 — 2026-09-04

- 修复：去掉「保存备份设置」后静态资源仍用 ?v=0.2.32，缓存的旧 settings.js 对已删除的 #bkCfgSave 绑事件导致打开分享链接报 TypeError

## 0.2.33 — 2026-09-04

- 新增：分享链接可选「需要登录查看」或持有链接即可打开
- 优化：Obsidian 仓库令牌只保留一键复制连接信息、重置、吊销
- 优化：知识库目录与编辑器纵向铺满页面剩余高度
- 变更：删除自建笔记仓库时永久删除其中的笔记与文件夹（高危确认）
- 优化：备份保留份数等设置自动保存；导出改用下载图标；支持导入备份 zip

## 0.2.32 — 2026-09-04

- 修复：知识库顶栏左侧箭头改为折叠应用侧边栏，不再收起笔记目录，避免文档大纲列错位
- 修复：登录页浏览器自动填充账号密码时触发 Uncaught TypeError（keydown 的 key 为空调用 toLowerCase）
- 优化：设置 → 功能设置 → Obsidian 同步区块间距与令牌行排版
- 修复：含图片笔记在实时编辑时因附件水合改写 img.src 触发重建而闪烁
- 修复：预览模式代码块被行内 code 样式盖成单色，现对围栏代码做语法高亮
- 优化：打开笔记后轮询远端更新，Obsidian 改同一篇时无需刷新即可看到同步内容
- 新增：笔记与文件夹可分享（期限、默认查看、可选编辑），目录显示「享」标记，公开链接可预览/按权限编辑
- 优化：回收站按文件夹树展开，支持按删除时间 / 修改时间 / 名称排序
- Obsidian 插件 0.0.4：大量删除点取消后暂停同步且不再弹窗；查看信息展示上次同步时间与策略；侧栏菜单增加插件设置与双向覆盖仓库（高危确认）；设置页一键粘贴万事屋连接信息
- 新增：仓库令牌支持一键复制（含服务端地址与 API Key）

## 0.2.31 — 2026-09-04

- 重构：知识库去掉「线上 / 同步 / 系统内置」分区，改为笔记仓库。系统默认创建「默认仓库」（可重命名、不可删除）与「系统内置」仓库（原内置文件夹与常驻笔记，不可删除、不可创建同步令牌）；用户可自建仓库、在仓库间切换
- 移除：浏览器「本地文件夹同步」（localsync）入口与实现，笔记同步统一走 Obsidian 插件、按仓库签发令牌
- 优化：回收站按所属笔记仓库分组，组内按原文件夹路径层级展示
- 设置：Obsidian 同步迁至「设置 → 功能设置」——总开关、插件下载、各仓库令牌管理、同步日志；镜像构建纳入 obsidian-plugin 目录供站内下载
- Obsidian 插件 0.0.3：侧边栏图标按连接成功/失败变色；点击可暂停同步、立即同步、打开万事屋、查看信息（插件版本 / 万事屋版本 / 地址 / 仓库）；设置页展示同步日志；握手接口 GET /api/sync/hello

## 0.2.30 — 2026-09-04

- 修复：在 Obsidian 中连续编辑时客户端误报「文件已被外部修改，正在自动合并更改」，已输入文字被部分回滚、光标跳到行首——根因是 2 秒 LWW 窗口把同一次连续保存当成站点优先并回拉旧正文，而服务端实际并无独立变更；推送现带上上次同步的站点 mtime，服务端未变则落地本地内容且不回拉，正文相同也不再调用 vault.modify，编辑内容与光标位置保持正常（插件 0.0.2）
- 优化：知识库目录悬浮文件夹/笔记时不再因操作按钮插入文档流而跳动，悬浮仅改变该项背景色，列表位置保持稳定
- 优化：Obsidian 同步覆盖笔记内引用的附件（图片、pdf、音视频、zip 等，单文件 ≤5MB）：推送时上传并改写为门户 /api/notes/assets 引用，拉取时下载到仓库并还原为 wiki 链接，附件与笔记引用关系保持正确；新增 /api/sync/asset、/api/sync/assets 端点

## 0.2.29 — 2026-09-04

- 修复：Obsidian 同步含 HTML/JS/SVG 的笔记时报「Expected ',' or '}' after property value in JSON at position N」并中断——本地 TestClient 复现证实服务端 FastAPI 直出的是合法 JSON（position 3581 处为字面 <svg 路径文本、Python json.loads 通过），而用户插件收到的却是同位置被 hex 化（6e636f64696e67… 解码即 ncoding="utf-8"?><svg）、content-type 响应头被剥离的响应体：根因是公网反向代理 / 云 WAF / 内容安全网关扫描响应正文，把 <script>、<svg>、<?xml、document.createElement("iframe") 等当成 XSS 攻击特征改写响应，令 JSON 在正文中途崩坏（0.2.28 判为「传输截断」不够准确，实为内容过滤型篡改）
- 方案：同步正文改走 base64 传输（双向）——GET /api/sync/file 返回 contentB64（base64(md 的 utf-8 字节)）取代明文 content；POST /api/sync/file 新增 contentB64 字段并优先解码、回退明文 content 兼容旧客户端；base64 后载荷只剩 [A-Za-z0-9+/=]，对内容过滤完全透明，WAF 无从下手。需服务端与插件一起更新（服务端 0.2.29 + 插件 0.0.1）
- Obsidian 同步插件启用独立版本线（初始 0.0.1，与服务端 app 版本解耦）：版本号打进 manifest.json、插件启动 console 日志与设置页顶部，用户反馈时可一眼确认所装版本；pullFile 增加护栏——响应既无 contentB64 也无 content 时抛错而非写入空正文，杜绝版本不匹配时清空本地笔记

## 0.2.28 — 2026-09-04

- 修复：Obsidian 同步插件在部分网络下同步到一半报「Expected ',' or '}' after property value in JSON at position N」——服务端 FastAPI 生成的 JSON 必然合法（已用 Python→JavaScriptCore 往返测试覆盖引号/反斜杠/控制符/emoji/U+2028/长文均正常），根因是响应字节经反向代理 / 公网链路被截断或错位；插件 HTTP 层加固：不再用 resp.json 隐式解析，改读 resp.text 手动 JSON.parse，失败自动重试 3 次（GET 幂等；POST 重试经服务端 LWW 判为 site-wins 不重复写，安全），仍失败则抛出带 status / content-type / 收到长度 vs content-length / 出错位置上下文片段的诊断信息——「收到长度 ≠ content-length」即传输截断铁证，便于精确定位是代理截断、编码错位还是网关错误页（仅 obsidian-plugin/main.js 变更，服务端无改动）

## 0.2.27 — 2026-09-04

- 新功能：Obsidian 双向同步插件——把门户知识库与 Obsidian 仓库双向同步（桌面 / 移动端），文件夹层级对应目录、笔记对应 .md；纯 JS 交付可直接 BRAT 侧载（obsidian-plugin/omnihome-sync/）
- 后端新增 /api/sync RESTful API：每用户一枚长期 API Key（X-API-Key 头，sha256 哈希存 config.json，明文仅生成时返回一次），list / file 读写 / rename / delete 等 8 个端点；path 用「文件夹/标题.md」逻辑寻址映射 bm_notes，不落物理镜像；映射规则与浏览器版 localsync.js 完全一致，两套同步互不破坏
- 同步策略：LWW 最后修改时间优先、2 秒同刻窗口内站点优先（真源永不被近乎同时或更旧的推送覆盖）；删除走软删进门户回收站（可恢复）；单轮删除 > 10 个由客户端弹确认；路径遍历 / 隐藏段（.obsidian/.git）/ 系统垃圾一律拒绝；同名笔记确定性分配 -2/-3 序号（多次 /list 稳定不抖动）
- 设置 → 数据与存储 新增「Obsidian 同步」卡片：生成 / 重置 / 吊销 API Key、复制服务端地址与 Key 明文
- 修复：数据库引擎（SQLite / PostgreSQL）下全新库写入笔记正文报 NOT NULL constraint failed: bm_notes.deleted——_sql_write_note_body 的 INSERT 遗漏 deleted/deleted_title 列（0.2.23 只给索引写入补了这两列），而 _table_ddl 把每列建成 NOT NULL；现补两列默认值。NAS 存量库因 ALTER ADD COLUMN 可空未暴露，全新建库才触发
- 文档：新增《Obsidian同步插件-服务端API》《Obsidian同步插件-用户使用指南》；DEPLOY.md 补充「Obsidian 同步」部署与公网访问安全说明（DB 方案无需挂载目录、移动端建议 HTTPS）

## 0.2.26 — 2026-09-04

- 修复：回收站弹层内「恢复」「永久删除」按钮无响应——事件委托误绑在目录树容器 #noteTree 上，而回收站列表位于独立弹层 #kbTrashMask 内（不在其子树），点击事件永远到不了监听器；改为 document 级委托
- 修复：回收站内点「清空」时确认弹窗被回收站弹层遮挡——两个弹层同为 z-index:100，确认框在 DOM 中靠前被后者覆盖；#confirmMask 提升至 z-index:260 永远置顶
- 修复：目录行布局——悬浮操作按钮（笔记 ⋯ / 文件夹 + 与 ⋯）由 opacity:0 常驻占位改为 hover 才占位（display:none ↔ grid），笔记数量统计真正贴右、标题占满整行仅在真正溢出时省略号
- 优化：回收站底部入口整条为点击热区（按钮撑满整行、悬浮变底色），不再缩成一团

## 0.2.25 — 2026-09-04

- 优化：知识库目录默认折叠——改为「展开集合」语义（localStorage 持久化），未展开过的文件夹一律折叠，点击展开并记住
- 优化：回收站入口移到目录树底部固定条（带计数徽标），笔记/文件夹拖到底部回收站条即可快速删除；原拖拽删除悬浮横条移除
- 优化：删除文件夹时，其中的笔记全部移入回收站（原来只移到上级文件夹）；恢复笔记时若原文件夹已删除会自动重建该文件夹
- 优化：回收站弹层内按原文件夹分组展示，未分组笔记单独归组，原文件夹已删除的组会标注提示
- 优化：文件夹行的笔记数量统计固定居右显示；目录行不再显示最近修改时间，笔记标题占满整行（时间等信息收进「查看详细」）
- 优化：笔记 ⋯ 操作菜单新增「查看详细」：弹窗展示最近更新时间、类型（常驻/只读标识）、字数（去空白统计）与大小（UTF-8 字节）
- 优化：嵌套文件夹缩进恢复为 CSS 嵌套 padding 传递（每层 12px，顶层树状竖线）

## 0.2.24 — 2026-09-04

- 修复：数据库引擎（SQLite / MySQL / PostgreSQL）下删除笔记不进回收站——根因是 _has_column 用 SELECT 列名 LIMIT 1 探测列存在性，而 SQLite 对空查询不校验列名恒返回成功，导致 bm_notes 旧表补列逻辑被永久跳过、软删字段（deleted/deleted_title）读写即 500 或静默丢列；改为 SELECT * LIMIT 0 读 cursor.description 通用判定，旧库启动即自动补齐两列。文件引擎不受影响
- 修复：本地同步把「每日计划」「灵感速记」等内置根文件夹挂进「同步笔记」分区后，在线上笔记区不可见也无法删除——syncRoots 计算时排除内置根，被同步过的内置文件夹回归「线上笔记」分区正常管理
- 修复：删除笔记提示「保留 N 天」与设置的实际天数不符——天数改为从 /api/notes/trash 下发的 trashDays 动态读取（0 显示为「未设自动清理」），未知时用中性文案
- 优化：回收站入口从侧栏分区改为树头按钮（原「多选」按钮位置，带计数徽标），点击弹层展示回收站列表，支持恢复 / 永久删除 / 一键清空；「多选模式」入口移入知识库 ⋯ 菜单
- 优化：多级嵌套文件夹缩进改为每行 inline padding-left 精确表达（每层 +14px），不再依赖父级 body 的 padding 累加，深嵌套时标题完整可见

## 0.2.23 — 2026-09-03

- BUG 修复：知识库多级嵌套文件夹时，由于 .kb-folder-body 每层都额外叠加 padding-left+margin-left+border-left（深 4 级即 100px），长标题（如「账号」「驾驶」「心理」「资产」「报表」）被挤压成 1-2 字截断；改为按层级只在第一层挂树状竖线、子层复用父级缩进，标题空间恢复
- BUG 修复：本地文件夹同步范围未收敛——常驻笔记（生词本 / 今日计划 / 灵感速记等内置项）以及用户主动移入「每日计划」「灵感速记」文件夹的笔记，会被原 reconcile 步骤 3 一并写入物理文件夹；现在以 pinned + 一级文件夹白名单做同步范围限定，reconcile 步骤 1（本地→站点）/ 2（本地消失 → 删站点）/ 3（站点→本地）三段都加 inSyncScope 守卫，残留 mapping 也会被回收
- BUG 修复：拖动「同步笔记」分区下的笔记到「线上笔记」分区后，UI 上仍藏在该分区不显示——因为 syncIds 集合没在移出时清理；新增 LocalSync.detachById / detachMany，把同步笔记移出本地区间时立即从 mapping + syncIds 抹除并触发对账，renderTree 即时回到「线上笔记」分区
- BUG 修复：拖动内嵌子文件夹到根的 .md 丢失层级——webkitGetAsEntry 递归时 walkEntry 没把父目录前缀透传，导致子文件夹里所有文件按 basename 全部进根目录（后端 import-files 因此建错路径、嵌套结构丢失）；现在 readEntries 接受 prefix 参数，子目录递归时叠加父名+'/'
- 新功能：知识库新增回收站分区。删笔记不再「不可恢复」，普通笔记软删进回收站，保留原文件夹与标题快照，可逐条恢复或永久删；常驻（pinned）笔记保持原行为（直接物理删除、_ensure_pinned 自动重建），不会进回收站
- 新功能：设置 → 数据与存储 → 「回收站」卡片中可设置保留天数（默认 30 天，0 = 仅手动清空不自动清），后端 GET /api/notes 入口懒清理过期项；回收站分区头部附加一键清空按钮，列表每行带恢复 / 永久删除两个图标按钮
- 后端新增 API：GET /api/notes/trash · POST /api/notes/{id}/restore · DELETE /api/notes/trash/{id} · POST /api/notes/trash/purge-all；trash 路由声明顺序在 /api/notes/{nid} 之前，避免 FastAPI 按声明顺序匹配把 'trash' 当 nid（沿用 v0.2.6 的路由顺序教训）
- 开发者文档：Dockerfile 补回阿里云 PyPI 镜像源（此前遗漏，NAS 在国内直连官方源构建极慢）；.gitignore 增加 .workbuddy/ 本地会话目录，避免工作记忆入库

## 0.2.22 — 2026-09-03

- 紧急修复：数据库引擎（PostgreSQL / MySQL / SQLite）下知识库页面加载即报「速记加载失败：请求失败 500」、刷新后持续无法加载——旧版「速记」文件夹改名合并到「灵感速记」时若两者并存会产生重复文件夹名，而文件夹表以（用户名, 文件夹名）为主键，全量写入时重复名撞唯一约束使笔记列表接口稳定 500；且改名因写入失败无法落库，形成每次刷新都复发的死循环。现在文件夹落库前保序去重，并在改名合并环节同步去重，双层根治；存量重复数据首次加载即自动合并自愈（文件引擎不受影响）

## 0.2.21 — 2026-09-03

- 修复：搜索下拉框内删除/隐藏历史记录时下拉被意外关闭——面板内按钮 mousedown 抢焦点叠加 DOM 重建使 :focus-within 失效，现已阻止焦点转移
- 修复：本地同步文件夹内子目录的笔记在目录树不可见——导入前未注册文件夹路径；现在导入先建目录，并在对账时自动补齐存量隐藏笔记的文件夹注册
- 修复：过长的大纲把文档大纲面板压缩至不可读——大纲条目锁定自然高度，超出在面板内滚动
- 修复：设为只读的笔记无法滚动——去除对编辑区的 pointer-events 屏蔽（它同时禁用了滚轮），编辑锁定改由 contenteditable=false 保证
- 优化：「我的笔记」分区改名「线上笔记」，新增「同步笔记」分区：本地同步相关的笔记与文件夹全部归入此区，标题可折叠
- 优化：文件夹悬浮删除钮改为 ⋯ 操作菜单：重命名（子树与笔记一并迁移）/ 复制文件夹（递归副本自动避重）/ 导出文件夹（zip，含子目录结构）/ 删除
- 优化：同步发生变更时显示状态通知，30 秒聚合一次不频繁打扰；逐条明细见同步面板日志

## 0.2.20 — 2026-09-03

- 新增：本地笔记双向实时同步（Chrome/Edge）——知识库 ⋯ 菜单「本地文件夹同步」授权本地目录后，本地 .md 增删改自动上站，站点编辑/重命名/删除实时落盘；句柄存 IndexedDB 刷新免重新授权，断网/关页期间修改恢复后自动补齐；冲突按最后修改时间优先（LWW），批量消失 >10 个自动暂停保护；Safari/Firefox 降级为导入/导出手动搬运；附《本地同步功能用户使用指南》
- 修复：导出 .md / 导出全部笔记下载到 {"detail":"未登录或会话已过期"} JSON——浏览器 <a> 下载带不了 Authorization 头，两个导出端点补支持 token 查询参数
- 优化：全局搜索历史支持管理——每条悬浮显 × 单条删除，标题行提供「隐藏」（可随时恢复）与「清除」全部历史

## 0.2.19 — 2026-09-02

- 修复：长笔记（内容出现滚动条）时标签页区域被压缩直至不可见——编辑器卡片限高后滚动主体强占空间，标签页/标题/工具栏/状态栏默认会被 flex 挤扁；现在除滚动主体外全部锁定自然高度，顶栏常驻可见

## 0.2.18 — 2026-09-02

- 优化：笔记悬浮 ⋯ 操作菜单新增「导出为 .md」，导出入口随顶栏移除一并收进菜单
- 优化：编辑器顶端去掉 Markdown 类型标识/导出/删除区，多笔记标签页直接置顶显示；删除入口保留在悬浮 ⋯ 菜单，只读提示芯片改挂标题上方右对齐

## 0.2.17 — 2026-09-02

- 修复：从本机拖图片到知识库目录后附件显示损坏——上传链路本身无损，根因是目录树的附件缩略图用裸请求取图被 401 拦截变裂图占位；缩略图改走带凭证的水合取图，彻底消除误报
- 修复：知识库多选按钮找不到入口——目录顶端新增显式「多选」开关，开启后普通点击即勾选笔记/文件夹/附件（不再必须按 ⌘/Ctrl），底部操作栏照常滑出；⌘/Ctrl 多选保留兼容
- 修复：开关文档大纲时编辑器宽度瞬跳——大纲面板改为宽度平滑塌缩/展开动画，编辑器随之平滑变宽变窄
- 优化：知识库目录顶端去掉「+」新建按钮（新建入口保留在分区与文件夹的 + 号菜单，顶部改为多选开关）
- 优化：编辑器顶部路径面包屑移至底部状态栏（保存状态与字数统计左侧），顶栏更清爽
- 新增：编辑器多笔记标签页——可同时打开多篇笔记，点标签切换、× 关闭；标签带只读锁标识，刷新后自动恢复上次打开的标签页
- 新增：笔记悬浮显示 ⋯ 操作按钮（贴行尾随文本自适应、不遮挡标题），菜单支持：在新标签页打开、重命名、创建副本、设为/解除只读、删除笔记；只读笔记标题与正文不可编辑、工具栏隐藏、目录与标签页带锁标识

## 0.2.16 — 2026-09-02

- 优化：表格选中行/列改为淡色底 + 主色描边，边界更清晰；行/列拖拽时目标侧显示 2px 虚线预览（按悬停上/下半区决定插入方位）
- 优化：实时模式光标可定位到表格之后——表格下方新增留白区，点击即落光标；末格按 Tab 跳出表格；表格在文末时自动补空行供落脚
- 优化：查找结果全部高亮（淡黄底）且当前定位项主色突出，编辑/预览/分屏均生效；编辑后防抖自动重标
- 优化：查找条新增显式的「↑ 上一个 / 下一个 ↓」按钮，循环跳转，Enter/Shift+Enter 同效
- 优化：文档大纲显示/隐藏改为滑入淡出动画
- 优化：知识库左侧目录新增多选操作栏——⌘/Ctrl 多选后底部滑出「已选 N 篇/个 + 删除 + 取消」，与拖拽挪位/拖入删除横条并存；附件支持 ⌘/Ctrl 多选，批量拖入编辑区一次性插入多个引用
- 优化：知识库目录区支持从本机直接拖图片进去自动上传为附件（自动归档附件分区）；附件预览灯箱去掉关闭/删除按钮，改点背景或按 Esc 关闭（删除入口保留在附件列表行）

## 0.2.15 — 2026-09-02

- 严重修复：登录即弹「Cannot read properties of undefined (reading 'toLowerCase)」——插件镜像同步/历史导入可能产出缺 name 字段的书签脏数据，前端搜索过滤直接崩溃；后端新增书签规范化（读取即修复存量脏数据并回写，缺 name 回退 url/「未命名」），前端书签/容器/生词本多处同步补齐空值兜底
- 严重修复：工具栏加粗/斜体/删除线/链接全部错位（符号插到选区外、自动多出占位文字）与分屏模式预览错落到编辑区下方——同一根因：编辑器初始化代码被重复执行，页内存在两个实时编辑实例，工具栏作用于错误实例、残留实例破坏分屏网格；已删除重复调用并给初始化加幂等守卫（重复进入先销毁旧实例）
- 功能：实时模式表格可直接编辑——单元格点击进入即可输入，Enter 下移（末行自动追加新行）、Tab 右移、Shift 反向；工具条不再常驻表格上方，改为点选行/列后在表格边缘浮出「上/下插行、删行」「左/右插列、删列」；鼠标悬浮行首/表头边缘显示拖拽手柄，可拖动调整行/列顺序
- 加固：表格单元格编辑实时回写源码且序列化无损，对齐方式保留；光标离开表格后自动补刷单元格内加粗/链接等内联渲染

## 0.2.14 — 2026-09-02

- 修复：加粗仅包裹部分选中文本、标题自动多出两行符号——根因 LiveMD.insertRawAt 对无换行的单行文本会拆成两行（parts[0]/parts[last] 重复），'# ' 因此变成 '## ' + 空行；改为单行文本直接拼入当前行
- 修复：插入表格后实时模式不渲染——LiveMD 原本没有块级表格支持，新增表格块渲染（识别 | 行 + 分隔行），含对齐、序列化无损还原
- 修复：Markdown 链接不实时渲染为可点击——LiveMD.inlineHtml 原本只支持图片/代码/强调，新增 [text](url) 行内链接与 <url> 自动链接渲染（文本可编辑，序列化经 data-pre/post 无损还原）
- 修复：分屏模式右侧预览无渲染——原因同链接/表格缺失（渲染走同一 mdRender，marked 已本地化），补全后分屏/预览均正常
- 修复：附件预览弹窗的关闭/删除按钮无响应、无法点背景关闭——根因 #assetLightbox 位于页面底部（晚于 notes.js 执行），直接绑定元素被 ?. 短路永不生效；改为 document 委托，并增加点背景空白关闭，删除确认改用统一 confirmModal
- 优化：编辑模式按钮文案改为「实时」，与 LiveMD 原地实时渲染语义一致
- 优化：文档大纲改为右侧固定栏（样式与左侧目录一致，sticky 置顶），打开笔记自动渲染展示，点击大纲按钮可隐藏，编辑器宽度随大纲存在自动适配
- 优化：实时模式表格操作——插入表格后自带工具条（+行 +列 -行 -列），数据行支持拖拽调整顺序，改动实时回写源码与预览
- 优化：编辑器标题栏与工具栏冻结固定——卡片高度受限、内容在 ed-body 内滚动，滚动编辑内容时标题/工具栏保持可见

## 0.2.13 — 2026-09-01

- 修复：知识库-工具栏加粗/斜体/链接/代码块按钮均无法使用——原版用 't === 标题' 等弱匹配且在 live 模式下 ta.value 重建会丢选区，改用 data-act 分发 + 源码模式 execCommand('insertText') 保留原生撤销栈 + live 模式用新加的 wrapSelection/lineInsert
- 修复：markdown 编辑器撤销快捷键无法使用——LiveMD 是 contenteditable，原生 Ctrl+Z 在程序化 rebuild 后失效，新增自有历史栈 + undo/redo API，Ctrl+Z/Shift+Ctrl+Z 在 live 模式接管，源码模式交给 textarea 原生栈
- 优化：去掉标题下方的文件夹胶囊（重复信息且不美观），保留面包屑表达位置
- 优化：左侧 kb-tree 在编辑器滚动时保持 sticky 置顶，不随内容滚走
- 优化：marked 库本地化（demo/js/vendor/），NAS 离线也能渲染完整 GFM；内置兜底渲染器补齐表格 / 行内链接 / 参考链接 / 自动链接 / 图片 / 嵌套列表等
- 优化：工具栏扩充——增加 H1/H2/H3 / 引用 / 有序列表 / 任务列表 / 行内代码 / 插入表格 / 大纲 / 查找替换
- 优化：新增文档大纲浮层：自动提取 H1-H6，点击跳转（切到分屏/预览并滚动到对应锚点）
- 优化：新增查找/替换条：支持 Enter/Shift+Enter 上一个下一个、Esc 关闭、替换一个/全部，live 模式下同步高亮目标行

## 0.2.12 — 2026-09-01

- 修复：知识库搜索框无法搜索、头像无法点击——kb-editor 自带的顶栏（含搜索/头像）始终未绑定，根因是 v0.2.8 3 栏重构后 .kb-editor-bar 内部 5 个 id 仍保留事件监听，冒泡到不存在的元素
- 修复：附件点击无法预览——HTML 上 #assetLightbox 带 hidden 属性，showAssetPreview() 只加了 .open 类没移除 hidden，CSS `asset-lightbox[hidden]{display:none}` 一直生效
- 修复：在文件夹外的笔记卡片没占满——.note-item 缺 width:100% 与 box-sizing:border-box
- 修复：导航折叠按钮点击无效果且图标丢失——v0.2.8 给 kb-editor 加了单箭头按钮但 .kb-editor-bar 已被本次删除；本次全局化后折叠按钮改用全局 #collapseBtn，在 kb 视图下语义切到“折叠 kb-tree”并旋转图标
- 优化：布局调整为全局 3 栏——删除 kb-editor 自带的 .kb-editor-bar 整个块，折叠按钮 / 知识库标题 / 日期 / 全局搜索 / 铃 / 头像全部复用全局 .topbar（与其他视图一致）
- 优化：kb-tree 与 kb-editor 平齐、纵向占满整个 .content 区域
- 优化：#collapseBtn 在知识库视图下旋转 180°表示“折叠 kb-tree”，折叠态回正表示“还能展开”，状态用 localStorage 持久化

## 0.2.11 — 2026-09-01

- 严重修复：知识库页面几乎所有按钮点击无反应（点笔记不切换、编辑/分屏/预览无法切换、目录里的 + 创建无效）——v0.2.8 重构布局时目录类名从 .note-layout 改为 .kb-layout，但 init() 里仍引用旧类名，取到 null 后抛 TypeError，导致其后全部事件绑定静默中断（树点击/编辑模式/工具栏/删除等一整段全部失效）。已删除该过时的折叠逻辑，全量扫描并清零其它悬空引用
- 修复：登录后弹出 $.features-admin-only.toggle is not a function——toggle 是 classList 的方法而非元素方法，改为直接设置 el.hidden
- 加固：上版新增的全局错误提示（红色 toast）正是本次定位问题的关键——两条报错直接指出了出错文件与行为

## 0.2.10 — 2026-09-01

- 修复：编辑器标题下方的文件夹标签被拉长占满整行——编辑器卡片改为 flex-column 后子元素默认被拉伸，给标签加 align-self:flex-start 收回内容宽度
- 加固：知识库 init() 里所有 DOM 调用补齐 ?. 空值防护——此前一处元素缺失会让后续全部事件绑定中断，表现成「所有按钮点了没反应」
- 加固：新增全局脚本错误可见化——未捕获的 JS 错误 / Promise 异常会以红色 toast 弹出（同一错误只提示一次），不再默默失灵难以排查

## 0.2.9 — 2026-09-01

- 修复：导入含 macOS 元数据的 zip 会把 ._*、__MACOSX/、Thumbs.db 全部当成笔记建出来，整树乱码、交互错乱——后端 import-md 增加过滤（__MACOSX/、._*、.DS_Store 等全部跳过）
- 功能：设置-数据与存储新增「一键清理」按钮（POST /api/notes/cleanup-junk），一键删除历史遗留的乱码笔记 / __MACOSX 文件夹及其内全部笔记，修复 v0.2.6 之前导入造成的污染
- 功能：知识库目录树支持从电脑拖拽 .md 文件 / 文件夹 / .zip 直接导入——单文件走 import-md 端点，文件夹用 webkitGetAsEntry 递归收集后走 import-files 端点（保留目录结构）
- 优化：目录树笔记项按设计图改为单行布局——左侧图标（普通笔记用 i-note、常驻笔记用 i-star）+ 中间标题（单行省略）+ 右侧修改时间，悬浮高亮整行；旧的悬浮删除按钮与多行副标题已隐藏（删除仍可通过底部 kb-trash 多选拖入，或打开笔记后在编辑区顶栏删除）

## 0.2.8 — 2026-09-01

- 修复：设置-关于卡的图标贴着卡片上边缘——card-pad 上边距为 0 而关于卡又没有 card-head，给 .about-pad 加上 padding-top 即可
- 优化：知识库视图按设计图重做 3 栏布局——左侧全局图标栏不动；中间 目录树（240px，自带 知识库 头部 + 计数 + 新建/更多）；右侧 编辑区（自带顶栏：返回/日期/搜索/铃/头像 + 面包屑 + Markdown + 导出/删除 + 工具栏 + 标题 + 正文 + 状态栏）
- 优化：知识库视图激活时全局顶栏自动隐藏（body[data-view=notes] .topbar{display:none}），避免与编辑区顶栏重复
- 优化：目录树头部新增 ... 溢出菜单收纳导入/导出，避免顶部太挤
- 优化：编辑区顶栏日期实时显示（月日 + 星期 + HH:MM），头像取当前用户首字母

## 0.2.7 — 2026-09-01

- 修复：升级后界面看不到变化（只有更新日志会变）——根因是 index.html 未设 Cache-Control，被浏览器或反向代理缓存，旧 HTML 继续引用旧版本的 js/css
- 优化：静态资源缓存策略——index.html 改为 no-cache 每次校验，带 ?v= 版本戳的 js/css 改为一年长缓存（发版换戳自动失效），从根上杜绝升级后前端不刷新

## 0.2.6 — 2026-09-01

- 修复：资源监控-硬盘下拉里出现 sda1、nvme0n1p1、loop0 等几十项——后端按 /sys/block 过滤只保留物理盘，跨平台用 psutil 兜底
- 优化：知识库视图顶部新增 导入/导出/新建 按钮（与目录里的 + 号菜单共用逻辑），入口不再藏在侧边栏
- 优化：编辑器顶部改为面包屑（知识库 / <folder path> / <note title>）+ Markdown 标识 + 导出/删除；单击根或文件夹段可跳转
- 优化：附件支持点击预览（灯箱大图，ESC 或点击空白处关闭），底部删除按钮一键清理
- 优化：附件支持拖入编辑区——drop 在光标处插入 markdown 引用（与 Alt+点击行为一致）
- 功能：单篇笔记导出为 .md（/api/notes/{nid}/export，文件名 RFC 5987 双形式支持中文）
- 功能：批量导出全部笔记为 zip（/api/notes/all，文件夹路径以子目录形式保留）
- 功能：批量导入 .md 或 .zip（/api/notes/import-md，逐级补齐父文件夹；首行是 H1 时取作标题，否则取文件名）
- 修复：FastAPI 路由声明顺序——/api/notes/{nid} 会把 'all'、'import-md' 当成 nid 吃，把静态路由提前到 get_note 之前避免冲突

## 0.2.5 — 2026-08-31

- 修复：系统监控-网络指标显示的是开机累计字节，且副值标签错把实时上传标成累计下载——改为真实速率（MB/s），按方向分别记录上次快照避免单次请求两次调用互相干扰，并修正文案对应数据
- 修复：资源监控-硬盘只看根分区，多盘 NAS 看不到其它硬盘——改为枚举 /sys/block 下的物理盘（Linux），自动映射挂载点取占用，未挂载或非 Linux 兜底返回根分区
- 修复：仪表盘网络标签网络累计（会话）为累计字节+端口变化累积而成，与实际值不符——改为网络 · 实时，单位与数值同步
- 优化：设置-功能设置对所有用户可见；开关仅管理员可调，普通用户看到当前状态+请联系管理员提示条
- 优化：系统监控-Docker 容器操作按钮（重启/停止/启动）从纯图标悬停显形改为带文字标签、始终可见，不再依赖 hover
- 优化：知识库目录折叠按钮移到左侧（固定在 .note-layout 左缘，目录折叠时也常驻可点），去掉了顶部常驻的新建笔记/新建文件夹工具条，新建入口由目录树内每个文件夹旁的 + 号菜单承担；菜单按主题风格重新设计（圆角/阴影/淡入动画）
- 优化：设置-关于卡重排版——拆成 Hero（Logo+名+CTA）、简介、功能胶囊（监控/导航/笔记/日程/保险库/工具）、系统信息四段，间距清晰不再拥挤

## 0.2.4 — 2026-08-31

- 紧急修复：v0.2.3 部署包漏打 backend/secretbox.py（AI 密钥加密模块），容器启动即 ModuleNotFoundError 并陷入重启循环——已补入文件
- 修复：打包清单改为自动收集 backend/*.py，不再手工罗列，从根上杜绝新增模块忘记入包
- 修复：打包脚本新增导入自检——解包后真正 import 一遍所有后端模块，漏文件当场报错，不会再等到容器崩溃才发现

## 0.2.3 — 2026-08-31

- 修复：知识库折叠目录后按钮随之被隐藏、再也打不开——折叠开关移到视图头部（折叠区之外），并按状态切换箭头方向与文案
- 修复：已登录状态下每次刷新都会先闪现登录页——登录页不再默认展开，改为启动遮罩；登录态校验的两个接口改为并行请求，停留时间减半
- 修复：跟随系统主题时，电脑切换深浅色必须刷新页面才生效——现在实时响应，切换即变，无需刷新
- 优化：登录页支持浅色 / 深色——首屏绘制前就按「上次选择 → 系统偏好」确定明暗，未登录也能看到符合系统主题的登录页，且不再有主题跳变
- 安全：AI 接口密钥改为服务端加密后落库（Fernet 认证加密），数据库与用户数据文件里不再出现明文；主密钥存于 data/secret.key（权限 600）且已排除出镜像与部署包
- 安全：历史明文密钥在下次读取时自动升级为密文；主密钥丢失时按「未配置」处理并提示重新填写，不会让服务崩溃

## 0.2.2 — 2026-08-30

- 修复：.md 文档图片依旧裂图——改为编辑器携带登录凭证取图后转为内嵌数据显示，不再依赖裸请求；页面静态资源加注版本戳，浏览器缓存旧脚本导致的反复失效一并根治
- 修复：在「我的笔记」分区新建文件夹却出现在分区外——目录仅保留「我的笔记 / 附件 / 系统内置」三个分区，普通文件夹与未分组笔记全部归入「我的笔记」
- 优化：新建入口统一——目录工具条与分区 / 文件夹加号点击后选择新建笔记或文件夹；笔记只允许建在「我的笔记」分区内
- 优化：目录折叠按钮、新建笔记 / 新建文件夹按钮与目录放在一起（目录区顶部工具条）
- 优化：鼠标悬浮笔记条目时右上角浮出删除按钮，点击即可删除
- 优化：拖拽笔记 / 文件夹到底部删除横条即可快速删除，支持多选批量（内置项自动跳过，文件夹删除前弹窗确认）

## 0.2.1 — 2026-08-30

- 修复：今日计划组件整体显示错乱崩溃——页面模板一处属性引号缺失导致后续标签被吞，现恢复正常
- 修复：.md 文档中添加的图片无法显示——图片请求未携带登录凭证被拦截，现编辑器与预览自动补齐鉴权；同时修复编辑器内图片地址在编辑过程中丢失的问题
- 修复：知识库未按住 ⌘/ctrl 直接点击笔记仍残留多选高亮，现普通点击自动退出多选状态
- 修复：多选笔记时选中框溢出遮挡相邻条目，现改用内描边不再外溢
- 优化：知识库新增「附件」分区——笔记中上传的图片 / 附件自动归档于此，点击附件即可把引用插入当前笔记（可在任意 .md 中显示）
- 优化：知识库「每日计划」文件夹归入内置区不再单独外置，内置文件夹统一带「内置」标识
- 优化：知识库「根目录」分区改名「我的笔记」，「常驻」分区改名「系统内置」（去掉星标）

## 0.2.0 — 2026-08-29

- 修复：知识库根目录区无法新建文件夹——根目录标题行新增「+」按钮，头部「新建文件夹」依旧建在根目录
- 修复：.md 编辑器标题 # 符号不再一直显示，默认隐藏、光标移到该行或悬停时才出现；分割线 --- / *** / ___ 现渲染为虚线
- 修复：.md 编辑器图片功能不可用——工具栏图片按钮可选本机图片，支持剪贴板粘贴图片、拖拽图片入编辑区，上传后自动插入
- 优化：括号配对改为输入光标移到括号旁即点亮另一半（不再依赖鼠标悬停）
- 优化：知识库目录折叠带展开/收起动画；文件夹可拖入其他文件夹或根目录，笔记/文件夹支持 ctrl/⌘ 点选多选后拖拽批量挪动
- 优化：知识库常驻区移至目录最底部，点击标题可整体隐藏；「灵感速记」文件夹归入常驻区展示（不提供手动新建入口）
- 优化：灵感速记创建的笔记自动存入「灵感速记」文件夹（原「速记」文件夹及内笔记自动改名迁移）；速记卡标题芯片点击即可给文档命名

## 0.1.9 — 2026-08-29

- 修复：仪表盘灵感速记输入区偏小、底部信息占用过多——输入区现占满剩余高度，字数统计贴底边；加载后状态不再停在「加载中」
- 修复：Markdown 预览中任务列表（- [ ]）被渲染成无序列表圆点加复选框，现只保留复选框
- 优化：快捷导航图标加载提速，加载成功过的图标按域名本地缓存，刷新页面/切换分类不再空白等待
- 优化：知识库新增「速记」专属文件夹，仪表盘速记保存其中，删除后自动重建
- 优化：灵感速记默认打开上次记录的笔记，头部新增「新建笔记」按钮（新建空白笔记存入速记文件夹）
- 优化：.md 编辑器代码块新增语法高亮（关键字/字符串/数字/注释），鼠标悬停在括号旁自动高亮另一半括号
- 优化：知识库支持多层文件夹（文件夹内可再建子文件夹），删除时笔记上移一层；左侧目录支持一键折叠（状态记住）

## 0.1.8 — 2026-08-29

- 修复：功能设置的下载扩展包按钮视觉上仍像置灰，现改为主题主色按钮，始终可点（扩展升级至 2.0.5）
- 浏览器扩展：不选分类收藏时默认落入「未分类」而非「常用」，避免万事屋中不存在常用分类时找不到书签
- 浏览器扩展：保存书签的更多选项与页内卡片新增标签输入框（回车 / 逗号逐个添加，点标签移除）

## 0.1.7 — 2026-08-29

- 修复：设置 → 功能设置 下载浏览器扩展提示未找到安装包——镜像构建时未将扩展包复制进容器，现 Dockerfile 已包含（扩展升级至 2.0.4）
- 设置 → 功能设置：浏览器扩展卡片显示可下载的扩展版本号；下载按钮不再置灰，不可用时点击给出原因提示
- 浏览器扩展：收藏按钮默认显示「收藏到万事屋」，选定分类后动态显示对应分类；各卡片间距调大；同步间隔恢复预设档位并新增自定义分钟输入（去掉上下箭头）；「选择分类」「打开万事屋」改为按钮样式

## 0.1.6 — 2026-08-29

- 修复：浏览器扩展首次同步备份报 URL.createObjectURL is not a function——MV3 后台 Service Worker 环境不提供 Blob URL，备份文件改用 data: URL 下载，同步流程恢复（扩展升级至 2.0.3）

## 0.1.5 — 2026-08-29

- 修复：浏览器扩展点击立即同步报 Cannot read properties of undefined (reading 'children')——书签树根节点 / 服务端返回数据缺少预期结构时未兜底，现统一防护并给出可读报错（扩展升级至 2.0.2）
- 浏览器扩展：自动同步间隔改为自定义分钟数输入（1 分钟 ~ 30 天），不再限定固定档位

## 0.1.4 — 2026-08-29

- 修复：浏览器扩展弹窗界面打开即被确认弹窗遮罩覆盖、全部按钮无反应——CSS 的 display 声明覆盖了 hidden 属性导致弹窗无法关闭，现以全局规则 + 内联样式 + 默认关闭处理器三重兜底，弹窗不可能再失控（扩展升级至 2.0.1）

## 0.1.3 — 2026-08-29

- 浏览器扩展 2.0：书签同步改为全量镜像浏览器书签栏——清空书签栏原有内容后按万事屋的分类与顺序原样重建，两端看到的内容完全一致
- 浏览器扩展：首次同步弹窗警告，确认确定后先自动下载一份当前浏览器书签备份（Netscape HTML，可直接导入回滚）再执行；登录后不再自动同步
- 浏览器扩展：右键「选择分类并保存」改为当前页面内浮层卡片完成（不再新开页面），支持改名称 / 选分类 / AI 分析（自动推荐分类、生成描述与标签）；未配置 AI 时卡片内给出相应提示
- 浏览器扩展：新增一键将浏览器书签导入万事屋（弹窗警告，执行前服务端自动备份）；新增「不同步隐藏的分类」开关
- 浏览器扩展：在万事屋页面新增 / 删除 / 修改书签或分类后即时探测并同步（内容脚本钩住页面写请求），打开插件弹窗时也会静默比对；修复单条地址非法的书签报 Invalid URL 中断整个同步（现跳过非法地址并显示跳过数量）
- 浏览器扩展：登录页「测试连接 / 登录」拆为两个按钮；修复登录成功后弹窗仍停留在登录页；界面排版间距放宽
- 设置 → 功能设置：新增「浏览器扩展」卡片，可一键下载扩展安装包（需部署包根目录附带 omnihome-extension.zip）

## 0.1.2 — 2026-08-28

- 仪表盘：所有组件支持拖拽调整大小——进入「编辑布局」后拖动卡片右下角手柄，横向按列吸附（3~12 列）、纵向按 20px 吸附，拖拽时实时显示「N 列 · N px」；双击手柄恢复该组件默认尺寸
- 仪表盘：尺寸偏好随布局一同持久化；窗口收窄到 1280px 以下时自动回落响应式默认布局，避免自定义跨列把卡片压扁
- 修复：天气详情弹窗中未来一周预报显示不全——弹窗加宽至 680px，日期列改为自适应折行（宽屏一行排满、窄屏自动上下排列），正文超高时内部滚动
- Docker 监控：新增容器内存占比饼图，按占用降序取前 8 个容器、其余合并为「其他」，悬浮高亮扇区并在圆心显示该容器占用明细；与容器筛选、搜索联动
- Docker 监控：容器指标采集由串行改为并行（线程池最多 16 并发），21 个容器场景实测由约 20 秒降至约 2 秒；新增单飞锁与 2 秒结果缓存，并发请求只采集一次，重复刷新瞬时返回
- 修复：管理员登录/注册/切换后需刷新页面才能看到「系统监控」入口——监控开关从会话接口注入改为随用户对象下发，登录/注册/切换/会话恢复四条路径统一生效
- 修复：容器内存饼图悬浮高亮每 5 秒被轮询重绘清空——保留悬浮状态并在重绘后恢复

## 0.1.1 — 2026-08-28

- 系统监控页面划分为「系统监控区」与「Docker 监控区」两个独立分区
- Docker 监控：新增筛选栏，支持下拉多选容器（含全选 / 清空），原容器搜索功能并入筛选栏，曲线 / 指标卡 / 容器表格统一跟随筛选联动
- Docker 监控：新增容器曲线组件，可切换各容器内存占用 / CPU 占用 / 网络上传 / 网络下载曲线，默认展示当前指标占用最高的前 5 个容器，带图例与悬浮数值
- Docker 监控：新增 CPU 占用率 / 内存占用率 / 网络上传 / 网络下载 四张合计指标卡，默认统计全部容器并跟随筛选联动
- 容器实时指标采集并行化，多容器环境下监控接口响应明显提速

## 0.1.0 — 2026-08-28

- 修复：点击日历日期不弹出计划编辑窗口（模块导出未挂载全局，日历联动守卫直接返回）
- 修复：Docker 容器列表内存 / 网络始终显示「—」，现按 cgroup 真实口径采集内存（扣除页缓存）、聚合各网卡流量折算实时速率，CPU 改为两次轮询间增量计算
- 系统监控改为管理员专属功能：默认关闭，管理员可在 设置 → 功能设置 中开启；相关接口同步仅限管理员访问，未开启时侧边栏与仪表盘不显示入口
- 新增资源监控：原「网络吞吐」升级为 CPU / GPU / 内存 / 网络 / 硬盘 五类曲线（可切换）；CPU 与 GPU 可选利用率 / 温度曲线，硬盘含读写速率与温度曲线、多盘可选；全部曲线带图例、坐标轴刻度，鼠标悬浮显示各数据点具体数值
- Docker 容器列表：支持按容器名 / 镜像模糊搜索，点击表头可按容器 / 状态 / CPU / 内存 / 网络 / 运行时长排序

## 0.0.9 — 2026-08-28

- 修复：仪表盘快捷导航选择「全部」分类时只显示部分磁贴，现与各分类一致展示全部书签
- 修复：天气城市搜索出现多条异常结果（各地同名村镇 / 街道），现仅保留可信的城市 / 区县结果；资溪县等区县无法搜索、抚州等无法模糊搜索，现新增 OSM 数据源并支持带 / 不带行政后缀（市 / 县 / 区）模糊匹配
- 修复：今日计划组件底部大量空白，现编辑区在上自适应拉伸、今日进度贴底边显示
- 天气详情：未来预报延长至一周，每日新增降雨概率展示
- 天气横条位置调整：从页面顶栏移至仪表盘顶部，与「编辑布局」按钮同一行（原描述文案移除）
- 今日计划改造：计划按月归档（每月一篇文档，存于知识库内置「每日计划」文件夹，不可删除）；每天首次登录自动在文档最上方生成今日空模板（含名言警句）；当月文档被删除后自动重建；仪表盘组件仅展示今日一节
- 日历联动：移除添加日程功能，改为点击日期弹出对应日期的计划编辑窗口（仅显示当天）；未来日期自动填充空模板，未来月份自动新建月度文档

## 0.0.8 — 2026-08-27

- 仪表盘：移除天气预报组件与大标题；新增顶栏天气横条——点击信息区查看详情（体感 / 风力 / 湿度 / 降雨概率 / 空气指数 / 紫外线 / 未来 5 日），操作区支持自定义横条显示内容（天气、最高最低温 / 体感、湿度、降雨概率、空气指数、紫外线指数）、位置设置（一键定位精确到区县，定位失败或未授权时可手动搜索城市）与手动刷新
- 顶栏日期时间精确到秒，并按天气所在地时区自动显示（手动选择城市 / 定位区县均自动匹配当地时间）
- 每日单词：改为随机出题；自动检测生词本，已收藏的单词不再出现；生词本条目新增中文释义与例句
- 快捷导航：多选拖拽时展示堆叠磁贴虚影与计数角标；拖拽期间底部横条变为删除落点，松手即批量删除
- 今日计划：仅保留上方编辑区域，移除下方渲染预览

## 0.0.7 — 2026-08-27

- 修复：设置 → 用户与账号 无法更换头像，现支持上传自定义头像（自动裁切压缩）与移除恢复默认
- 修复：登录设备 / 位置 / 时间数据不准确，现每次登录真实记录（设备 = 系统 + 浏览器、位置按 IP 识别本机 / 局域网、时间为实际登录时刻），保留最近 10 条并支持单条删除
- 修复：切换用户时普通用户无法加载用户列表，现改为手动输入用户名 + 密码验证切换；验证成功后两账号双向绑定，有效期内（30 天）免密切换，过期后重新输入密码验证即可恢复绑定
- 设置 → 用户与账号：移除邮箱字段；多用户管理权限分级——普通用户可见已绑定账号并手动解绑，管理员可查看全部账号
- 快捷导航：新增书签多选（⌘+点击或左上角勾选圈），选中后拖动到左侧分类即可快速归类，支持批量删除
- 仪表盘快捷导航组件：去掉工具分类标签，仅保留书签分类
- 数据存储：所有数据表与字段补齐中文注释（MySQL / PostgreSQL 引擎生效），便于数据库侧维护查看

## 0.0.6 — 2026-08-27

- 修复：版本更新日志弹窗内容过长时撑破显示范围，现正文区域内部滚动
- 修复：快捷导航页大卡片的快捷键标号遮挡标题，现角标回归行内布局不再叠压
- 设置 → 数据与存储：存储引擎选择数据库时改为结构化配置表单（数据库类型 / 地址 / 端口随类型自动带出 / 用户名 / 密码 / 库名），支持单独测试连接，密码留空提交自动沿用已保存密码
- 设置 → 关于：新增系统介绍与开发人员信息，系统信息卡新增存储引擎展示
- 设置中心：移除右下角取消 / 保存更改按钮，外观类设置调整后自动保存生效，其余分区保留各自独立保存按钮
- 快捷导航：分类改为左侧纵向排版（含书签计数，窄屏自动回落横向）；「全部」与「未分类」内置分类也支持隐藏与恢复
- 仪表盘快捷导航组件：保留悬浮切换分类，新增点击锁定效果——锁定后悬浮其它分类不再切换，再次点击被锁定分类解锁

## 0.0.5 — 2026-08-27

- 修复：PostgreSQL 引擎下服务启动崩溃（desc 为保留关键字，建表 SQL 未引用转义），现所有建表 / 读写 / 删除 / 迁移 SQL 标识符统一双引号引用，外部数据库引擎全模块正常建表与读写
- 兼容性：双引号引用标识符在 PostgreSQL、MySQL（非 ANSI_QUOTES 模式）、SQLite 三种引擎下均合法，文件 / SQLite 引擎行为不变

## 0.0.4 — 2026-08-27

- 存储层重构：按功能模块拆分数据表（书签 / 分类 / 笔记 / 日历 / 保险库 / 偏好 / 备份配置等 14 张模块表），每表配备 (用户, 模块主键) 主键与常用查询索引，大数据量下不再单表混存互相拖累
- 旧版单表数据自动无损迁移：升级后首次连接数据库自动完成迁移，迁移前全量快照备份、单事务原子执行、失败自动回滚并保持双读兜底
- 三存储引擎（文件 / SQLite / 外部数据库）统一按模块分表存储，切换引擎时自动双向迁移全部用户数据
- 性能优化：大数据量书签 / 日历场景下模块读取与统计聚合提速，多用户数据相互隔离、互不拖累
- 修复：存储引擎切换时目标库残留旧数据可能混入迁移结果，现于迁移前自动清空目标用户数据

## 0.0.3 — 2026-08-27

- 设置 → 数据与存储：支持切换存储引擎（文件 / SQLite / 外部数据库 MySQL、PostgreSQL），切换时自动双向迁移全部用户数据
- 设置 → 数据与存储：新增定时自动备份（周期可选）、最多保留份数（超出自动覆盖最旧）、备份列表内一键恢复
- 设置 → 数据与存储：存储空间统计改为打开时实时刷新，新增内容无需刷页即可看到
- 清空数据改为主题风格弹窗，需输入登录密码确认后方可执行
- 快捷导航：快捷键按分类独立编号，每个分类前 9 个书签各自编入 ⌘+1~9
- 快捷导航：页面书签改为信息完整的大卡片（图标 / 名称 / 地址 / 描述 / 标签 / 分类）；分类支持隐藏与恢复
- 快捷导航：新增标签多选下拉筛选；页面分类改为点击切换（仪表盘组件保持悬浮切换）
- 仪表盘快捷导航组件简化为图标 / 标题 / 地址，标题过长自动省略，悬浮可查看完整信息

## 0.0.2 — 2026-08-27

- 新增独立登录页（不再模糊展示站内内容）；设置 → 用户与账号 新增「开放新用户注册」开关，开启后新用户可在登录页自行注册
- 快捷导航：新增书签导入 / 导出（标准 Netscape HTML 格式，兼容浏览器书签）；新建书签弹窗网址置顶，填写后自动识别名称与图标
- 修复：书签全部删除后不再自动重生默认书签
- 修复：站点图标改由后端代理获取，解决部分网络下无法自动填充的问题
- 修复：底部错误提示改显示“叉”图标并转红；AI 连接测试支持直接用未保存的表单配置测试

## 0.0.1 — 2026-08-26

- 首次发布：仪表盘、快捷导航、知识库、今日计划、每日单词、天气、系统监控、密码保险库、实用工具箱、全局搜索
- 快捷导航：分类管理（新建 / 重命名 / 删除 / 拖拽排序）、书签拖拽排序与跨分类移动、⌘+1~9 按书签顺序快速打开、站点图标自动获取与自定义图标、描述与标签、AI 智能填充
- 仪表盘：编辑模式、组件拖拽排序（支持任意方向）、组件收纳与恢复
- 设置中心：外观与主题、多用户管理、备份与导出、AI 智能配置（兼容 OpenAI 格式）、版本信息与更新日志

