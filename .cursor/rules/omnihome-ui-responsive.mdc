---
description: 凡 UI 改动必须同时适配 PC 与手机，禁止只改一侧
globs: demo/**/*.{css,html,js}
alwaysApply: true
---

# 万事屋 UI：PC / 手机双向适配

改 `demo/` 里任何用户可见界面（布局、样式、路由壳、导航、弹层、编辑器）时，**必须同时考虑 PC 与手机**，不能只在当前视口看起来对就算完成。

## 断点（与代码一致）

| 视口 | 约定 |
|---|---|
| **PC** ≥769px | 完整侧栏（≥1024）或图标栏（769–1024）；无抽屉、无底栏 |
| **手机** ≤768px | Phone Shell：顶栏汉堡打开**抽屉侧栏**；内容单列 |
| ≤1024px | 侧栏收为图标栏（桌面窄窗） |
| ≤1280px | 仪表盘卡片跨列重排 |

以 `demo/css/mobile.css` 的 `@media (max-width: 768px)` 与 `body.is-phone` 为准，不要另起一套互斥断点。

## 导航（不要回退）

- 手机**只保留侧边导航**（抽屉）。禁止再加常驻底部 Tab / `phone-nav`。
- 手机入口：顶栏 `#collapseBtn`（汉堡）→ `.sidebar` 抽屉；设置/退出走侧栏用户卡片或顶栏菜单。
- PC 入口：常驻侧栏；折叠为图标栏，不要变成抽屉。

## 改布局时必查

1. **对侧不回归**：改手机样式不得打乱 PC 的 `.app` 双栏、知识库三栏、仪表盘网格；改 PC 不得让 ≤768 出现横溢、遮挡、点不到的控件。
2. **固定层 vs 内容**：`position: fixed` 贴底/贴顶的元素必须给主内容留空，或让内部 flex 把状态栏留在可视区内。`.editor { overflow: hidden }` 时，滚动主体（`.ed-body` / `.livemd`）在窄屏要用 `min-height: 0`，**禁止**用过大的 `min-height` 把 `.ed-foot`（面包屑、字数）裁掉。
3. **安全区**：底部留 `var(--om-safe-bottom)`，顶部留 `var(--om-safe-top)`；高度用 `var(--om-app-h)`（100dvh），不要裸 `100vh` 当手机全屏高。
4. **触控**：可点目标约 ≥44px；输入框手机上 16px，避免 iOS 聚焦缩放。
5. **同一状态跨页**：改了写入/派生状态，把读它的其它视图也点一遍（仪表盘速记 vs 知识库、分享页等）。

## 完成前怎么验

至少两种宽度：

- 手机：375×667 或 390×844（≤768）
- PC：1280×800 或更大

相关页面各走一遍主路径（含笔记编辑底栏、侧栏开关、仪表盘）。没有真机/浏览器时，用带真实 CSS 的静态页量 `getBoundingClientRect`，确认关键条不在视口外、不被其它层盖住。
