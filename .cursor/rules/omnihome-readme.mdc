---
description: 用户可感知的功能改动必须同步更新 README 和必要截图
alwaysApply: true
---

# 万事屋：功能改动同步 README

新增、调整或下线用户能感知的能力时，**同一次改动**里更新仓库根目录 `README.md`，让文档和产品一致。这包括页面、交互、默认开关、入口位置、部署方式、浏览器扩展和 Obsidian 插件。

纯内部重构，对外行为和界面都不变时，不必改 README。

## 写什么

1. **功能说明**：改对应章节里的能力、入口路径、默认是开还是关。删掉已经不存在的说法。
2. **截图**：该功能的主路径、主导航或布局变了，就更换或新增 `docs/images/` 下的图，并在 README 里用相对路径引用。图注写清是哪个页面。PC 约 1440×900，手机约 390×844。图中用本地演示数据，不要出现真实密钥、生产账号或私人内容。
3. **结构与部署**：目录、配置项、扩展或插件的安装步骤变了，改对应小节。
4. **不要**把 `CHANGELOG.md` 的条目抄进 README。更新日志仍只维护 CHANGELOG；README 描述当前产品。

## 和发版的关系

发版流程见 `.cursor/rules/omnihome-release-push.mdc`。功能说明进 README，变更条目进 CHANGELOG，版本号进 `backend/app_version.py`。三处各写各的，功能改动不能只改版本号不改 README。
